'use client'

export default function ProjectCard({ props }) {
    return (
        <div className="flex flex-col items-center justify-center w-full h-full p-4 bg-[#0F1020] rounded-lg shadow-lg">

        </div>
    )
}